//create a login function that checks the mysql database for the user and password and sets the session cookie

function login () {
    //check the database for the user and password
    //if the user and password are correct, set the session cookie
    //if the user and password are incorrect, return an erro

}

module.exports = {
    login
}